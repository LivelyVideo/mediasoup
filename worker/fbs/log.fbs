namespace FBS.Log;

table Log {
    data: string (required);
}

table WriteFailedNotification {
    source: string (required);
    error: string (required);
    file: string (required);
    data: string (required);
}

